<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="jp.co.deliv.android.imasugu.sugusake"
      android:versionCode="1" android:versionName="0.1.0">
      
    <application android:icon="@drawable/icon"
    	android:label="@string/app_name"
    	android:theme="@android:style/Theme.Light"
    	android:debuggable="false">
    
        <activity android:label="@string/app_name" android:name="SuguSakeTopView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:label="@string/app_name" android:name="ShopListView">
        </activity>

        <activity android:label="@string/app_name" android:name="ShopDetailView">
        </activity>

        <activity android:label="@string/app_name" android:name="SakeMapView">
        </activity>

        <activity android:label="@string/app_name" android:name="SuguSakeAboutView">
        </activity>
        
        <!-- AdMobアクティビティ -->
		<activity android:name="com.google.ads.AdActivity"
			android:configChanges="keyboard|keyboardHidden|orientation" />

		<uses-library android:name="com.google.android.maps" />
		
    </application>
    
    <uses-sdk android:minSdkVersion="4" />

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
		
</manifest> 